name: Deploy Rules changes

on:
  push:
    branches:
      - master
    paths:
      - 'waf/**'
      - '!waf/README.md'
      - '!rules/docs/**'

permissions:
  contents: read

# One writer to the rules at a time; see octodns-deploy.yaml for the reasoning.
# A separate group from DNS, because the two surfaces do not contend.
concurrency:
  group: octorules-deploy
  cancel-in-progress: false

env:
  CLOUDFLARE_API_TOKEN: ${{ secrets.PKGHAUS_INFRASTRUCTURE_CLOUDFLARE_API_TOKEN }}

jobs:
  deploy:
    name: Publish Rules config
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
          cache: 'pip'
      - run: pip install -r requirements.txt
      - name: Publish to providers
        uses: doctena-org/octorules-sync@a278ab9ed29451d7560c9caa9b26d0bf9f8cb9e9  # v1.8.6
        with:
          config_path: octorules.yaml
          doit: --doit
          force: 'Yes'
          lint: 'Yes'
          audit: 'Yes'
